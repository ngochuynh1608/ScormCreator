# Local overrides: Caddy on 8080/8443 so host :80/:443 are free.
# Usage: npm run docker:up

services:
  caddy:
    ports: !override
      - "8080:80"
      - "8443:443"
      - "8443:443/udp"
    environment:
      CADDY_SITE: localhost
      CADDY_EMAIL: ""
