# Local overrides: Caddy on 8080 (HTTP only — no auto-HTTPS redirect to :443).
# Usage: npm run docker:up

services:
  caddy:
    ports: !override
      - "8080:80"
    environment:
      # http:// prefix disables automatic HTTPS for local Docker
      CADDY_SITE: http://localhost
